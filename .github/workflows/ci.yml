name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  go_lint_and_test:
    name: Code lint and tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Set up go
        uses: actions/setup-go@v6
        with:
          go-version: '^1.24'
      - name: Run gofmt check
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "Code is not formatted. Run 'gofmt -w .'"
            exit 1
          fi
      - name: Run Go Linter
        uses: golangci/golangci-lint-action@c7c1219093bae1f72610c5fd1a51a674647863f8
        with:
          version: latest
      - name: Run tests
        run: go test ./...
  codespell:
    name: Check for spelling
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: codespell-project/actions-codespell@8f01853be192eb0f849a5c7d721450e7a467c579
        with:
          ignore_words_list: "actualy,nwe"
          skip: 'go.mod,go.sum'
